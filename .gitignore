# Helm
# Ignore build artifacts from `helm dep update` (packaged subcharts), but not
# vendored subchart source — those are checked in intentionally.
charts/*/charts/*.tgz
charts/*/Chart.lock

# Editor
.vscode/
.idea/
*.swp
.DS_Store

# AI assistant — personal/local settings only
# (shared guidance files like CLAUDE.md / AGENTS.md can be committed intentionally)
CLAUDE.local.md
.claude/
.codex/
.cursor/
.aider*
